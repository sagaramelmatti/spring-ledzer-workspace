import { ItemWiseSaleHistory } from './item-wise-sale-history';

describe('ItemWiseSaleHistory', () => {
  it('should create an instance', () => {
    expect(new ItemWiseSaleHistory()).toBeTruthy();
  });
});
