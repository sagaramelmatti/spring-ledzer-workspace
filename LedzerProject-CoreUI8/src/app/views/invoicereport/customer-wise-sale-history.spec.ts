import { CustomerWiseSaleHistory } from './customer-wise-sale-history';

describe('CustomerWiseSaleHistory', () => {
  it('should create an instance', () => {
    expect(new CustomerWiseSaleHistory()).toBeTruthy();
  });
});
